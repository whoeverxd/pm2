---
const testimonials = [
  {
    quote: 'No teníamos una web formal y eso nos hacía ver improvisados. Ahora nuestros pacientes encuentran la información rápido y nos escriben directo por WhatsApp.',
    name: 'Mariana Rojas',
    role: 'Odontóloga',
    image: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&w=400&q=80',
  },
  {
    quote: 'Necesitábamos una presencia digital seria para presentar nuestros servicios de ingeniería. El nuevo sitio nos ayuda a cotizar con más confianza.',
    name: 'Daniel Herrera',
    role: 'Ingeniero consultor',
    image: 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?auto=format&fit=crop&w=400&q=80',
  },
];
---

<section class="section relative">
  <div class="container-x">
    <div class="grid gap-12 lg:grid-cols-12 lg:gap-10">
      <div class="lg:col-span-4">
        <div class="eyebrow mb-5">Testimonios</div>
        <h2 class="section-title">
          La confianza no se promete. Se construye con resultados visibles.
        </h2>
        <p class="mt-6 section-kicker max-w-md">
          Cuando la web explica mejor tu servicio, el negocio empieza a verse distinto.
        </p>
      </div>

      <div class="lg:col-span-8 grid gap-5 md:grid-cols-2">
        {
          testimonials.map((testimonial) => (
            <article class="panel panel-hover flex h-full flex-col justify-between p-8 sm:p-10">
              <p class="text-xl leading-9 tracking-[-0.03em] text-white sm:text-2xl sm:leading-10">
                “{testimonial.quote}”
              </p>

              <div class="mt-10 flex items-center gap-4 border-t border-white/10 pt-6">
                <img
                  src={testimonial.image}
                  alt={testimonial.name}
                  class="h-14 w-14 rounded-full object-cover"
                  loading="lazy"
                />
                <div>
                  <div class="font-semibold tracking-[-0.02em] text-white">{testimonial.name}</div>
                  <div class="text-sm text-brand-muted">{testimonial.role}</div>
                </div>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </div>
</section>
